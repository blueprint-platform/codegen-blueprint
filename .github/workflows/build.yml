name: Build & Test

on:
  push:
    branches: [ main ]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        java: [ '21', '25' ]

    name: Build & Test (JDK ${{ matrix.java }})

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven (tests + coverage)
        run: mvn -B -q -ntp clean verify

      - name: Verify generated project (hexagonal strict, sample basic)
        env:
          JAVA_VERSION: ${{ matrix.java }}
          BOOT_VERSION: '3.5'
          ARTIFACT_ID: greeting-j${{ matrix.java }}-b35-hex-strict
          PACKAGE_NAME: io.github.blueprintplatform.greetingj${{ matrix.java }}b35hexstrict
        run: |
          chmod +x ci/verify-gen-hex-sample-strict.sh
          ./ci/verify-gen-hex-sample-strict.sh

      - name: Verify generated project (standard strict, sample basic)
        env:
          JAVA_VERSION: ${{ matrix.java }}
          BOOT_VERSION: '3.5'
          ARTIFACT_ID: greeting-j${{ matrix.java }}-b35-std-strict
          PACKAGE_NAME: io.github.blueprintplatform.greetingj${{ matrix.java }}b35stdstrict
        run: |
          chmod +x ci/verify-gen-std-sample-strict.sh
          ./ci/verify-gen-std-sample-strict.sh

      - name: Verify generated project (hexagonal strict, no sample)
        env:
          JAVA_VERSION: ${{ matrix.java }}
          BOOT_VERSION: '3.5'
          ARTIFACT_ID: empty-j${{ matrix.java }}-b35-hex-strict
          PACKAGE_NAME: io.github.blueprintplatform.emptyj${{ matrix.java }}b35hexstrict
        run: |
          chmod +x ci/verify-gen-hex-nosample-strict.sh
          ./ci/verify-gen-hex-nosample-strict.sh

      - name: Verify generated project (standard strict, no sample)
        env:
          JAVA_VERSION: ${{ matrix.java }}
          BOOT_VERSION: '3.5'
          ARTIFACT_ID: empty-j${{ matrix.java }}-b35-std-strict
          PACKAGE_NAME: io.github.blueprintplatform.emptyj${{ matrix.java }}b35stdstrict
        run: |
          chmod +x ci/verify-gen-std-nosample-strict.sh
          ./ci/verify-gen-std-nosample-strict.sh

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: |
            target/site/jacoco/jacoco.xml
            target/site/jacoco-it/jacoco-it.xml
          flags: codegen-blueprint
          name: codegen-blueprint
          fail_ci_if_error: false