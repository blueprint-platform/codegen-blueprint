coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%
    patch:
      default:
        target: 0%
        threshold: 100%

comment:
  layout: "reach, diff, flags, files"
  behavior: default

ignore:
  - "target/**"
  - "**/target/**"
  - "**/generated/**"
  - "**/generated-sources/**"

flags:
  codegen-blueprint:
    paths:
      - "src/main/java/"
      - "src/test/java/"